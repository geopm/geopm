{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "https://geopm.github.io/geopm_pbs_hook_config.schema.json",
  "title": "geopm openpbs hook config",
  "type": "object",
  "properties": {
    "node_profile_name": {
      "description": "Which profile name to use for node performance models",
      "type": "string"
    },
    "max_power": {
      "description": "Maximum power, in watts, represented by the configured power-performance models",
      "type": "number"
    },
    "profiles": {
      "description": "Map of profile names to performance models",
      "type": "object",
      "additionalProperties": {
        "anyOf": [
          {
            "description": "An individual profile containing a quadratic node performance model",
            "type": "object",
            "properties": {
              "model_type": {
                "description": "Type of performance model. Defaults to original-quadratic when omitted.",
                "type": "string",
                "enum": ["original-quadratic"]
              },
              "model": { "$ref": "#/definitions/quadratic_model_coefficients" }
            },
            "additionalProperties": false
          },
          {
            "description": "An individual profile containing quadratic host performance models",
            "type": "object",
            "properties": {
              "model_type": {
                "description": "Type of performance model. Defaults to original-quadratic when omitted.",
                "type": "string",
                "enum": ["original-quadratic"]
              },
              "hosts": {
                "description": "A map of host names to performance models",
                "type": "object",
                "additionalProperties": {
                  "description": "An individual host containing a host performance model",
                  "type": "object",
                  "properties": {
                    "model": { "$ref": "#/definitions/quadratic_model_coefficients" }
                  },
                  "additionalProperties": false
                }
              }
            },
            "additionalProperties": false
          },
          {
            "description": "An individual profile containing a piecewise-linear node performance model",
            "type": "object",
            "properties": {
              "model_type": {
                "type": "string",
                "enum": ["piecewise-linear"]
              },
              "model": { "$ref": "#/definitions/piecewise_linear_model" }
            },
            "required": ["model_type", "model"],
            "additionalProperties": false
          },
          {
            "description": "An individual profile containing piecewise-linear host performance models",
            "type": "object",
            "properties": {
              "model_type": {
                "type": "string",
                "enum": ["piecewise-linear"]
              },
              "model": { "$ref": "#/definitions/piecewise_linear_model" },
              "hosts": {
                "description": "A map of host names to piecewise-linear performance models",
                "type": "object",
                "additionalProperties": {
                  "description": "An individual host containing a piecewise-linear performance model",
                  "type": "object",
                  "properties": {
                    "model": { "$ref": "#/definitions/piecewise_linear_model" }
                  },
                  "additionalProperties": false
                }
              }
            },
            "required": ["model_type"],
            "additionalProperties": false
          }
        ]
      }
    }
  },
  "definitions": {
    "quadratic_model_coefficients": {
      "type": "object",
      "required": [ "x0", "A", "B", "C" ],
      "properties": {
        "x0": { "description": "Independent variable offset in the performance model", "type": "number" },
        "A": { "description": "2nd-degree coefficient in the performance model", "type": "number" },
        "B": { "description": "1st-degree coefficient in the performance model", "type": "number" },
        "C": { "description": "Dependent variable offset in the performance model", "type": "number" }
      },
      "additionalProperties": false
    },
    "piecewise_linear_model": {
      "description": "A mapping of power limit (watts) to average FOM at that power level. Keys are power limits as strings, values are FOM numbers.",
      "type": "object",
      "additionalProperties": {
        "type": "number"
      }
    }
  },
  "additionalProperties": false
}
